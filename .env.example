# EvoAgentBench — copy to .env and set as needed
# Judge: use Vertex Gemini for soft metrics (default when running with --run)
EVOAGENTBENCH_JUDGE_LLM=vertex

# Vertex AI Search (retrieval/search tool): data store ID from your GCP project
# EVOAGENTBENCH_VERTEX_SEARCH_DATA_STORE=your-data-store-id
# EVOAGENTBENCH_GCP_PROJECT=your-project-id
# EVOAGENTBENCH_GCP_LOCATION=global

# Real tool API: call an HTTP endpoint for a tool (e.g. get_weather)
# EVOAGENTBENCH_TOOL_GET_WEATHER_URL=https://your-cloud-function.run.app/weather
